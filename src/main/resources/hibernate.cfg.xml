<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- ========== Database Connection Settings ========== -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/fsadexam?useSSL=false&amp;serverTimezone=UTC&amp;allowPublicKeyRetrieval=true
        </property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">Meghana@0706</property>

        <!-- ========== Hibernate Dialect ========== -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- ========== Connection Pool Settings ========== -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- ========== Schema Auto-Management ========== -->
        <!-- update: creates table if not exists, updates schema on change -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- ========== SQL Logging ========== -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.use_sql_comments">true</property>

        <!-- ========== Entity Class Mappings ========== -->
        <mapping class="com.klef.fsad.exam.Service"/>

    </session-factory>
</hibernate-configuration>
